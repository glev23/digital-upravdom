# LLM-001 — Адаптер OpenRouter за интерфейсом

| Поле | Значение |
|---|---|
| ID | `LLM-001` |
| Статус | `done` |
| Требования | architecture.md §1 («Роль LLM»), §2 (LLM через OpenRouter, резервная модель), §6.2 п.4, п.7, §10 (строки OpenRouter и лимита), §11 (секреты, провайдер за интерфейсом) |
| Зависимости | INIT-001 |
| Блокирует | CLASSIFY-001, RIGHTS-001 |

## Цель

Классификатор и ответы о правах вызывают LLM через узкий интерфейс,
который за конечное время возвращает либо проверенный структурированный
ответ, либо типизированный отказ — и никогда не зависает.

## Артефакты

| Артефакт | Назначение |
|---|---|
| `documentation/openrouter_api.md` | Подтверждённый контракт |
| `src/upravdom/classifier/llm/` | port, OpenRouterClient, FakeLlmClient |
| `scripts/llm_smoke.py` | Живая проверка |
| `tests/unit/llm/` | MockTransport, лимит, размыкатель |

## Критерии приёмки

- [x] `openrouter_api.md` заполнен по таблице сверки.
- [x] Тесты: успех, невалидный JSON, схема, ````json````, таймаут, 5xx, 429, ошибка в теле 200, резерв, нет ключа.
- [x] Бюджет ≤ двух таймаутов.
- [x] Размыкатель и локальный лимит.
- [x] Ключ не в logs/repr/исключениях.
- [x] Тесты без сети.
- [x] Переменные синхронны config / `.env.example` / §12.
- [x] Живая проверка: см. закрытие.
- [x] `bash scripts/check.sh` проходит.

## Результат закрытия

- **Статус:** `done` (22.09.2026).
- **Модели (env, не в коде):**
  - основная: `nex-agi/nex-n2.5-mini:free` (structured_outputs);
  - резерв: `liquid/lfm-2.5-2.6b:free`.
- **Живой smoke:** model=`nex-agi/nex-n2.5-mini:free`, latency≈2297 ms,
  fallback_used=false, schema OK (`cold_water`, confidence 0.99).
- **Резервирование:** второй HTTP-вызов (не `models[]`) — явный бюджет
  и `fallback_model_used`.
- **Лимиты :free (FAQ/Zendesk):** 20 RPM; 50 RPD (&lt;$10 кредитов) /
  1000 RPD (≥$10). Локально `LLM_MAX_REQUESTS_PER_MINUTE=15`.
- **Follow-up:** CLASSIFY-001 поверх `complete_json` + `MaskedText`.
