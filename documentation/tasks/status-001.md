# STATUS-001 — Сквозной статус заявки и уведомление подписчиков

| Поле | Значение |
|---|---|
| ID | `STATUS-001` |
| Статус | `done` |
| Исполнитель | dev-ilya (ветка `dev-ilya`) |
| Требования | architecture.md §5.2 (`ticket_events` append-only, `ticket_subscribers`), §7 (исходящие только через очередь), §6.5 (подписчики после склейки); idea_and_scope.md As Is п. 4 («заявка передана дальше — для жителя теряется»), To Be п. 3, MoSCoW (Should Have) |
| Зависимости | TICKET-001, FLOW-001 |
| Блокирует | PRES-001 (сценарий «статус не теряется при передаче») |

## Цель

Житель не звонит, чтобы узнать, что с заявкой: каждый переход статуса и
каждая передача между организациями («передано в РСО», «передано
подрядчику») приходят ему сообщением сами, а по номеру заявки видна
история переходов с датами. Это закрывает боль из As Is — «после передачи
в РСО заявка для жителя теряется» — и должно работать для **всех**
подписчиков заявки, включая присоединённых дедупликацией.

## Scope

### Входит

- уведомление всех подписчиков о смене статуса (`change_status`);
- переадресация заявки между организациями (`reroute`) с событием
  `routed` и уведомлением;
- история заявки по номеру: последние события с датами;
- расширение служебного CLI для демонстрации переходов и переадресации;
- тексты уведомлений.

### Не входит

- рабочее место диспетчера / HTTP API для смены статуса — нет по
  architecture.md §8 (объявленный API порождает обязательства сдачи);
- эскалация при просрочке срока (ГЖИ и т. п.) — follow-up;
- уведомления об отключениях — NOTIFY-001;
- отписка жителя от уведомлений по заявке — follow-up.

## Технические требования

**Уведомление о смене статуса.** В `tickets/service.py:change_status` —
после записи события `status_changed`, **в той же транзакции**, в очередь
`outbox` ставится сообщение каждому подписчику заявки
(`ticket_subscribers`). Почему в той же транзакции: статус без
уведомления или уведомление без статуса — рассинхрон, который житель
увидит («сказали, что выполнено, а в статусе — в работе»). Исходящие —
только через `outbox.enqueue_message` (§7), прямая отправка запрещена.

Кому отправлять:

- всем подписчикам заявки, у которых **действующее согласие** на ПДн
  актуальной версии (`onboarding.service.has_valid_consent`) — после
  отзыва согласия бот жителю больше не пишет;
- адрес отправки — `chat_id` диалога с жителем. В `users` хранится только
  `max_user_id`; для диалога с ботом `chat_id` ≠ `user_id`
  (подтверждено живым событием, фикстура `tests/fixtures/max/`). Отправка
  по `user_id` в MAX поддерживается (`POST /messages?user_id=…`,
  max_api.md §7), но в `outbox`/`HttpxMaxClient` сейчас только
  `chat_id` — **добавить адресацию по `user_id`** (payload `outbox`
  `{"user_id": …}` + параметр в `MaxClient.send_message`), а не угадывать
  `chat_id`. Альтернатива — сохранять `chat_id` у пользователя при каждом
  входящем событии; выбрать одно и записать решение в закрытие.

Текст: «Заявка № N: статус изменён — <новый статус>. <адресат, если
изменился>». Для `completed` — «Заявка № N выполнена. Если проблема не
решена — напишите, создам новую заявку.» Кнопка «Статус заявки».

**Переадресация** — `tickets/service.py:reroute(ticket, *, zone, org_id,
actor)`: меняет `routed_to_org_type`/`routed_to_org_id` (и
`responsibility_zone`, если зона другая), пересчитывает адресата через
`resolve_addressee` при переходе на РСО по ресурсу, пишет событие
`routed` с `payload = {"from": …, "to": …, "reason": …}` и уведомляет
подписчиков: «Заявка № N передана: <организация>, телефон <контакт>».
Статус при переадресации не меняется (переадресация и статус — разные
оси; «передано подрядчику» — это статус `routed_to_contractor`, а
«передано в РСО» — смена адресата). Закрытая заявка не переадресуется.

**История по номеру.** «статус 1024» (FLOW-001) дополнительно показывает
последние 5 событий заявки из `ticket_events` в хронологическом порядке:
«22.09 14:05 — принята (УК «…»)», «22.09 15:10 — передана в АО
«Водоканал»», «22.09 18:00 — выполнена». Время — в `DISPLAY_TIMEZONE`.
События `subscriber_joined`/`subscriber_left` жителю показываются
агрегированно («к заявке присоединились ещё 3 жителя»), без имён и
идентификаторов других людей.

**Служебный CLI.** `scripts/ticket_status.py` уже меняет статус — теперь
это приводит к уведомлениям. Добавить `scripts/ticket_reroute.py <номер>
--zone rso|uk [--org-id …]` для демонстрации переадресации. Новый скрипт
**не нужно** копировать в Docker-образ, если он запускается только
локально; если через `docker compose exec app` — добавить в `COPY`
`docker/app.Dockerfile` (урок DATA-001: скрипт не попал в образ, контейнер
падал).

**Тексты** — `tickets/texts.py`.

## Сценарии

| # | Действие | Ожидаемое |
|---|---|---|
| 1 | `ticket_status.py N in_progress` | Автор получает «статус — в работе» |
| 2 | У заявки 3 подписчика (после DEDUP-001) | Уведомление получают все трое |
| 3 | Один подписчик отозвал согласие | Ему уведомление не уходит, остальным — уходит |
| 4 | `ticket_reroute.py N --zone rso` | Событие `routed`, адресат — РСО по ресурсу дома, уведомление с контактом РСО |
| 5 | «статус N» | Статус + история последних 5 событий с датами |
| 6 | Запрещённый переход | Исключение, ни события, ни уведомления |
| 7 | Сбой посреди `change_status` | Откат: ни статуса, ни уведомления |
| 8 | `completed` | Текст с предложением написать снова |

## Артефакты

| Артефакт | Назначение |
|---|---|
| `src/upravdom/tickets/service.py` | Уведомления в `change_status`, `reroute`, чтение истории |
| `src/upravdom/tickets/notify.py` | Рассылка подписчикам через `outbox` (с проверкой согласия) |
| `src/upravdom/tickets/texts.py` | Тексты уведомлений и истории |
| `src/upravdom/bot_gateway/outbox.py`, `max_client.py` | Адресация по `user_id` (или решение с сохранением `chat_id`) |
| `src/upravdom/flow/handlers.py` | История в ответе на «статус N» |
| `scripts/ticket_reroute.py` | Служебная переадресация |
| `tests/integration/test_ticket_status_notify.py` | Сценарии 1–8 |

## Критерии приёмки

- [x] Сценарии 1–8 покрыты тестами и проходят.
- [x] Уведомление и смена статуса — одна транзакция (тест на откат).
- [x] Уведомления получают все подписчики с действующим согласием, и только они.
- [x] Отправка адресована правильно (по `user_id` или сохранённому `chat_id`), решение записано в закрытии.
- [x] В истории, которую видит житель, нет данных других жителей.
- [x] `bash scripts/check.sh` проходит.

## Проверка

```bash
uv run pytest tests/integration/test_ticket_status_notify.py -v
bash scripts/check.sh
```

## Результат закрытия

- **Статус:** `done` (22.09.2026), локально без деплоя на VM, ветка `dev-ilya`.

- **Изменённые артефакты:**
  - `src/upravdom/tickets/notify.py` — новый: получатели (подписчики с
    действующим согласием) и постановка уведомлений в `outbox`;
  - `src/upravdom/tickets/service.py` — уведомление в `change_status`,
    новая `reroute`, `list_history_events`, `count_joined_subscribers`,
    `is_history_event`, исключение `TicketClosed`;
  - `src/upravdom/tickets/routing.py` — `org_display` (разбор полиморфного
    адресата, был продублирован в `flow/handlers.py`);
  - `src/upravdom/tickets/texts.py` — тексты уведомлений и ленты,
    склонение «житель/жителя/жителей»;
  - `src/upravdom/bot_gateway/outbox.py`, `max_client.py` — адресация по
    `user_id`;
  - `src/upravdom/flow/handlers.py` — история в ответе на «статус N»,
    переход на общий `org_display`;
  - `scripts/ticket_reroute.py` — новый CLI; добавлен в `COPY`
    `docker/app.Dockerfile`;
  - `tests/integration/test_ticket_status_notify.py` — 11 тестов;
  - `documentation/architecture.md` — §7 (два способа адресации),
    «История решений».

- **Решение по адресации (вопрос из постановки).** Выбран вариант с
  `user_id`, а не сохранение `chat_id` у пользователя: он не требует
  миграции и работает для жителя, который после последнего рестарта боту
  ещё не писал (важно для NOTIFY-001 — там рассылку инициирует
  планировщик, а не входящее событие). Способ адресации различается ключом
  `user_id` в `payload` очереди — тем же приёмом, что `callback_id`;
  `outbound_messages.max_chat_id` (NOT NULL) остаётся ключом доставки и
  порядка и хранит в этом случае тот же `max_user_id`. `MaxClient.
  send_message` принимает ровно один адресат, иначе `ValueError`.

- **Фактические проверки:**
  - `uv run pytest tests/integration/test_ticket_status_notify.py` — 11
    passed (сценарии 1–8 + адресация по `user_id`, отсечение служебного
    `routed` из ленты, отказ переадресовать закрытую заявку);
  - `bash scripts/check.sh` — OK, **338 passed, 2 skipped** (было 327);
    ruff, `ruff format`, `mypy --strict` (99 файлов) чистые;
  - живой прогон на дев-базе, заявка № 1, три подписчика (автор, сосед,
    сосед с отозванным согласием): `ticket_status.py 1 in_progress` →
    `ticket_reroute.py 1 --zone rso` → `ticket_status.py 1 completed` дали
    **6 сообщений в `outbound_messages` — 2 получателя × 3 события**, у
    отозвавшего согласие ноль; все адресованы по `user_id`;
  - ответ на «статус 1» живьём:

    ```
    № 1 — выполнена. Адресат: АО «Водоканал Казань» (тест). Допустимый перерыв
    по нормативу — до 23.09.2026 00:11 (ПП РФ №354, приложение 1, п.1).
    История:
    • 22.09 20:11 — принята
    • 22.09 20:11 — в работе
    • 22.09 20:11 — передана: АО «Водоканал Казань» (тест)
    • 22.09 20:11 — выполнена
    • к заявке присоединились ещё 2 жителя
    ```

  - `docker compose build app` + `docker compose exec app python
    scripts/ticket_reroute.py --help` — скрипт в образе есть (урок
    DATA-001 проверен явно).

- **Отклонения от постановки:**
  1. **Служебное `routed` из `create_ticket` в ленту не попадает.** Такое
     событие пишется, когда РСО по ресурсу не нашлась и заявка ушла на УК;
     для жителя никакой передачи не было, строка «передана: УК» сразу
     после «принята» вводила бы в заблуждение. Событие отличается по ключу
     `to` в payload — его пишет только `reroute`. Покрыто тестом.
  2. **Число присоединившихся считается по `ticket_subscribers`**
     (`is_author = false`), а не по событиям `subscriber_joined`: после
     выхода из склейки (DEDUP-001) число должно уменьшаться, а
     `ticket_events` append-only. Тип события `subscriber_left` здесь не
     заводится — он в миграции `0007` DEDUP-001.
  3. **Переадресация в зоны `owner`/`municipality` запрещена** (`TicketError`):
     адресата у них не существует, заявка осталась бы без получателя. CLI и
     так принимает только `uk|rso`.
  4. **Кнопка «Статус заявки» в уведомлении собирается локальным импортом**
     `upravdom.flow.callbacks` внутри функции: `flow` зависит от `tickets`,
     и импорт на уровне модуля замкнул бы цикл через `upravdom.flow.__init__`.
     Дублировать формат `clf:st:<номер>` в `tickets` хуже — разъедется при
     первой правке FLOW-001.
  5. **Сценарии 1 и 4 проверены на уровне сервиса** (`change_status`,
     `reroute`), а не вызовом CLI из теста: скрипты открывают собственную
     сессию через `session_scope()` и коммитят, то есть писали бы мимо
     откатываемой транзакции фикстуры. Сами CLI прогнаны живьём (выше).
  6. `sqlalchemy.func` и `Settings` добавлены в импорты `tickets/service.py`;
     `_org_display` в `flow/handlers.py` стал тонкой обёрткой над общим
     `tickets.routing.org_display` — дубль разбора адресата убран.

- **Follow-up:**
  - эскалация при просрочке `due_at` (вне scope, отмечено в постановке);
  - отписка жителя от уведомлений по заявке;
  - живой прогон в MAX на VM — вместе с батч-деплоем основного разработчика;
  - NOTIFY-001 переиспользует `outbox.enqueue_message(user_id=…)` и
    `tickets.notify` — дублировать механизм не нужно.
