# RIGHTS-001 — Ответы на вопросы о правах жителя со ссылкой на норму

| Поле | Значение |
|---|---|
| ID | `RIGHTS-001` |
| Статус | `done` |
| Исполнитель | dev-ilya (ветка `dev-ilya`) |
| Требования | architecture.md §6.4 (путь независим от классификации; при недоступности LLM — честное сообщение), §4 («уверенная выдумка»), §10, §11 (маскирование, журналы); idea_and_scope.md MoSCoW (Should Have); кейс — работа с официальными данными, недопустимость выдавать предположение модели за факт |
| Зависимости | CLASSIFY-001 (порт LLM, поиск KB, маскирование, проверка ссылок по номеру фрагмента), KB-001 |
| Блокирует | PRES-001 |

## Цель

Житель спрашивает не «у меня течёт», а «могут ли отключить горячую воду
на месяц?», «положен ли перерасчёт, если две недели холодные батареи?»,
«за сколько должны предупредить об отключении?» — и получает короткий
ответ человеческим языком **только** на основе пунктов нормативов из базы
знаний, с явными ссылками на эти пункты. Если в базе нет нормы, которая
отвечает на вопрос, — бот честно говорит, что не может ответить, и
подсказывает, куда обратиться. Выдуманная норма в ответе хуже любого
отказа: житель пойдёт с ней в УК или ГЖИ.

## Scope

### Входит

- модуль `src/upravdom/rights/`: определение, что сообщение — вопрос о
  правах, поиск фрагментов, генерация ответа, проверка основания, отказ;
- вход в сценарий из диалога FLOW-001 **до** классификации;
- журнал ответов (миграция);
- тексты, тесты, ручной скрипт проверки на живой модели.

### Не входит

- изменение промпта и схемы классификатора (`classifier/prompt.py`,
  `schema.py`) — там сейчас работает основной разработчик (CLASSIFY-003);
- семантический кэш ответов — follow-up;
- расширение корпуса нормативов (перерасчёт — раздел IX ПП №354 и др.) —
  отдельная задача KB; отсутствие нормы в базе — повод для отказа, а не
  для ответа «из головы» модели;
- юридическая консультация — ответ сопровождается оговоркой.

## Технические требования

**Как отличить вопрос о правах от жалобы — без изменений классификатора.**

1. Явный вход: `/rights <вопрос>`, `/права <вопрос>`, «вопрос: …».
   Команда без текста — подсказка «Напишите вопрос после команды,
   например: /права могут ли отключить горячую воду на месяц?».
2. Узкая эвристика в `rights/detect.py`: вопросительная форма **и**
   начало с оборотов о правах/обязанностях — «могут ли», «имеют ли право»,
   «обязаны ли», «должны ли (меня|нас) предупредить», «положен ли», «какой
   срок», «сколько (можно|дней|часов)», «как (получить|сделать)
   перерасчёт», «куда жаловаться».
   **Не** считаются вопросом о правах и уходят в классификацию: «кто
   должен…», «кто отвечает…», «кому звонить…» — это вопрос «кому», ядро
   продукта (CLASSIFY-001); и любые описания проблемы, даже с «?» в конце
   («опять нет воды?»).
   Отрицательные примеры — обязательная часть тестов.

Проверка выполняется в `flow/handlers.py:on_message` после команды статуса
и до классификации. Мгновенное подтверждение («Принял, определяю…»)
для вопроса о правах — своё: «Ищу в нормативах…».

**Ответ.**

- текст вопроса маскируется (`mask()` → `MaskedText`) до поиска и LLM;
- поиск — `knowledge.search(masked, k=5)` с фильтром действующей редакции и
  `management_company_id` дома жителя;
- LLM — общий клиент процесса `get_llm_client()` (не создавать новый:
  иначе не работают лимит частоты и размыкатель, урок ревью CLASSIFY);
- схема ответа (pydantic):

| Поле | Тип | Правило |
|---|---|---|
| `sufficient` | bool | `false`, если во фрагментах нет ответа на вопрос |
| `answer` | str ≤ 700 символов | Простым языком, без номеров актов и пунктов в тексте — ссылки добавляет код |
| `cited_fragments` | list[int] | Номера показанных фрагментов `[1..N]`, на которые опирается ответ (тот же приём, что в CLASSIFY-001, `confirm_citations`) |

- промпт — `rights/prompt.py`, `PROMPT_VERSION` своя: отвечать **только**
  по показанным фрагментам; если их недостаточно — `sufficient = false`;
  не упоминать нормы, которых нет во фрагментах; не давать советов «на
  всякий случай».

**Проверка основания — отказ вместо ответа, если:**

- `sufficient = false`;
- после `confirm_citations` не осталось ни одного подтверждённого фрагмента;
- в `answer` встречается номер акта (`№\s?\d+`) или «п. N», которого нет
  среди подтверждённых фрагментов (модель проговорилась о норме, которой
  ей не показывали).

Отказ: «В нормативах, которые я знаю, прямого ответа на этот вопрос нет.
Вы можете уточнить в своей управляющей компании (АДС: <телефон>) или в
жилищной инспекции вашего региона.» Никакого частичного ответа модели.

**Формат ответа жителю:**

```
<answer>

Основание:
• ПП РФ №354, Прил. 1 п. 4
• ПП РФ №354, п. 31 пп. н

Это справка по нормативам, а не юридическая консультация.
```

Ссылки — `RetrievedChunk.label` подтверждённых фрагментов (полная ссылка
с документом), а не короткий `ref`.

**Отказы инфраструктуры.**

| Отказ | Поведение |
|---|---|
| `LlmError` (любой) | Честно: «Сейчас не могу подобрать ответ по нормативам — попробуйте позже.» + контакт АДС УК. Ответа по прототипам или по кэшу здесь нет (§6.4: функция отключается) |
| Qdrant недоступен | То же сообщение, без вызова LLM |

Основной сценарий (жалобы) не затрагивается ни одним из этих отказов.

**Журнал** — новая таблица `rights_logs` (миграция `0009`):
`id`, `inbound_event_id` (FK), `question_masked`, `refused` (bool),
`refusal_reason` (`insufficient` / `no_citations` / `unknown_norm_in_answer`
/ `llm_unavailable` / `kb_unavailable`), `used_chunk_ids` (JSON),
`model_name`, `prompt_version`, `kb_version`, `latency_ms`, `created_at`.
Только маскированный текст вопроса; текст ответа модели **не** хранится
(может пересказывать вопрос) — достаточно ссылок и признака отказа.

**Известная дыра (CLASSIFY-003).** Бесплатная модель
`nex-agi/nex-n2.5-mini:free` нестабильно заполняет `cited_fragments` —
для RIGHTS это будет означать частые отказы. Это ожидаемо: **не**
ослаблять проверку основания, чтобы «поднять» долю ответов, — выбор
модели решается в CLASSIFY-003. В тестах — `FakeLlmClient`.

**Ручная проверка** — `scripts/rights_demo.py "могут ли отключить
горячую воду на месяц?"` (по образцу `scripts/classify_demo.py`): ответ,
ссылки, признак отказа, задержка. Запускать только с ключом OpenRouter из
`.env`; вызовы тратят суточный лимит бесплатной модели — несколько
прогонов, не циклы.

## Сценарии

| # | Сообщение | Ожидаемое |
|---|---|---|
| 1 | «могут ли отключить горячую воду на месяц?» | Ответ со ссылкой на ПП №354, Прил. 1 п. 4 |
| 2 | «/права за сколько должны предупредить о плановом отключении?» | Ответ со ссылкой на ПП №354, п. 31 пп. н |
| 3 | «кто должен чинить стояк?» | **Не** вопрос о правах → классификация (FLOW-001) |
| 4 | «опять нет воды?» | Не вопрос о правах → классификация |
| 5 | Вопрос, на который в базе нет нормы | Отказ, без ответа модели |
| 6 | Модель ссылается на несуществующий фрагмент | Отказ (`no_citations`) |
| 7 | В тексте ответа — «ПП №290», которого не показывали | Отказ (`unknown_norm_in_answer`) |
| 8 | LLM недоступна | Честное сообщение + контакт АДС |
| 9 | «/права» без текста | Подсказка с примером |
| 10 | Вопрос с телефоном и ФИО | В LLM и журнал — только маскированный текст |

## Артефакты

| Артефакт | Назначение |
|---|---|
| `db/migrations/versions/0009_rights_logs.py` | Таблица `rights_logs` |
| `src/upravdom/models/` | Модель `RightsLog` |
| `src/upravdom/rights/detect.py` | Команды и эвристика вопроса о правах |
| `src/upravdom/rights/prompt.py`, `schema.py` | Промпт, `PROMPT_VERSION`, схема ответа |
| `src/upravdom/rights/service.py` | Маскирование → поиск → LLM → проверка основания → ответ/отказ → журнал |
| `src/upravdom/rights/texts.py` | Тексты ответа, отказов, оговорки |
| `src/upravdom/flow/handlers.py` | Вход в сценарий до классификации |
| `scripts/rights_demo.py` | Ручная проверка на живой модели |
| `tests/unit/rights/`, `tests/integration/test_rights.py` | Детектор (с отрицательными примерами), сценарии 1–10 |

## Критерии приёмки

- [x] Сценарии 1–10 покрыты тестами и проходят (`FakeLlmClient`).
- [x] Детектор не перехватывает вопросы «кто/кому» и описания проблем (отрицательные тесты).
- [x] Ответ без подтверждённого фрагмента или с «чужой» нормой в тексте не показывается жителю.
- [x] Ссылки в ответе — полные (`label`), только подтверждённые.
- [x] В LLM и в `rights_logs` — только маскированный текст; текст ответа модели не хранится.
- [x] Используется общий LLM-клиент процесса.
- [x] Миграция `0009` применяется и откатывается.
- [x] Живой прогон `rights_demo.py` на 3–5 вопросах — результат (включая отказы) в закрытии.
- [x] `bash scripts/check.sh` проходит.

## Проверка

```bash
uv run alembic -c db/alembic.ini upgrade head
uv run alembic -c db/alembic.ini downgrade -1 && uv run alembic -c db/alembic.ini upgrade head
uv run pytest tests/unit/rights tests/integration/test_rights.py -v
uv run python scripts/rights_demo.py "могут ли отключить горячую воду на месяц?"
bash scripts/check.sh
```

## Результат закрытия

- **Статус:** `done` (23.09.2026), локально без деплоя на VM, ветка `dev-ilya`.

- **Изменённые артефакты:**
  - `db/migrations/versions/0009_rights_logs.py` — таблица `rights_logs`;
  - `src/upravdom/models/observability.py`, `enums.py` — модель `RightsLog`,
    перечисление `RefusalReason`;
  - `src/upravdom/rights/detect.py` — команды и узкая эвристика;
  - `src/upravdom/rights/prompt.py`, `schema.py` — промпт `rights-1`, схема
    ответа;
  - `src/upravdom/rights/service.py` — маскирование → поиск → LLM → проверка
    основания → ответ/отказ → журнал;
  - `src/upravdom/rights/texts.py` — ответ, отказы, оговорка;
  - `src/upravdom/flow/handlers.py` — вход в сценарий до классификации, своё
    подтверждение «Ищу в нормативах…»;
  - `scripts/rights_demo.py`;
  - `tests/unit/rights/`, `tests/integration/test_rights.py` — 63 теста.

- **Фактические проверки:**
  - `alembic upgrade head` → `downgrade -1` → `upgrade head` — `0009`
    применяется и откатывается;
  - `uv run pytest tests/unit/rights tests/integration/test_rights.py` —
    **63 passed** (40 на детектор, из них 17 отрицательных; 10 на проверку
    основания; 13 сценариев 1–10 через `flow.on_message`);
  - `bash scripts/check.sh` — OK, **448 passed** (было 385);
  - живой прогон — ниже.

- **Живой прогон `rights_demo.py`, 5 вопросов (23.09.2026).** Ключ OpenRouter
  передан пользователем; основная модель `nex-agi/nex-n2.5-mini:free`
  нестабильна и часть вызовов ушла на резервную `liquid/lfm-2.5-2.6b:free`.

  | # | Вопрос | Результат | Основание |
  |---|---|---|---|
  | 1 | могут ли отключить горячую воду на месяц? | ответ | ПП РФ №354, Прил. 1 п. 4 — **верно** |
  | 2 | за сколько должны предупредить о плановом отключении? | ответ | ПП РФ №354, п. 31 пп. н — **верно** |
  | 3 | сколько часов можно без холодной воды по нормативу? | ответ | ПП РФ №354, Прил. 1 п. 1 — **верно** |
  | 4 | какой срок устранения аварии на внутридомовой системе? | ответ | ПП РФ №416, п. 13 — **верно** |
  | 5 | положен ли перерасчёт, если две недели холодные батареи? | ответ | ПП РФ №354, Прил. 1 п. 1 — **НЕВЕРНО** |

  **Ответов 5 из 5, из них один неверный по существу** и это главный вывод
  задачи. В вопросе №5 речь об отоплении и перерасчёте, а модель уверенно
  ответила по норме о перерыве **холодного водоснабжения** и заключила, что
  перерасчёт положен. Формально правило соблюдено: фрагмент существует и был
  показан. По существу — нет: раздела о перерасчёте (раздел IX ПП №354) в
  корпусе нет, и правильным поведением был бы отказ, но модель вместо
  `sufficient = false` взяла ближайший показанный фрагмент.

  **Вывод:** проверка основания гарантирует, что норма существует и была
  показана модели, но **не** что она относится к вопросу. Ослаблять проверку
  нельзя, но и полагаться на неё как на защиту от неверного ответа тоже.

  Первый прогон был сделан на более строгом правиле («любая ссылка на норму в
  тексте ответа — отказ») и дал **2 ответа из 5 при 3 отказах**
  `unknown_norm_in_answer`: модель цитировала как раз показанные ей нормы.
  Правило приведено к постановке; цифры обоих вариантов оставлены здесь,
  чтобы выбор «покрытие против осторожности» можно было пересмотреть
  осознанно.

- **Отклонения от постановки:**
  1. **Правило «чужой нормы» сверяет номера, а не запрещает ссылки вовсе.**
     Постановка говорит «номер акта или „п. N", **которого нет** среди
     подтверждённых фрагментов»; первая реализация отказывала при любом
     упоминании и давала ложные отказы (цифры выше). Проверка не ослаблена:
     выдуманный пункт по-прежнему не показывается жителю, на это есть
     отдельные тесты (`tests/unit/rights/test_rights_basis.py`).
  2. **Пустой результат поиска — отказ `no_citations` без вызова LLM.**
     В постановке такой ветки нет, но звать модель, когда показывать нечего,
     значит просто тратить лимит и провоцировать ответ «из головы».
  3. **`RefusalReason` — общее перечисление в `models/enums.py`**, а не
     локальное для модуля: значение попадает в CHECK колонки, и держать его
     рядом с остальными enum'ами модели данных последовательнее.
  4. **Эвристика требует вопросительный знак.** «Должны были предупредить, но
     не предупредили» — это жалоба, и заводить по ней справку вместо заявки
     было бы ошибкой. Явная команда `/права` работает и без знака вопроса.
  5. **Подсказка при команде без текста не пишется в журнал** и не тратит
     вызов LLM: обращения как такового ещё нет.
  6. **`scripts/rights_demo.py` не добавлен в образ** — как и
     `classify_demo.py`: он запускается локально с ключом из `.env`, внутри
     контейнера не нужен.

- **Follow-up (в порядке важности):**
  1. **Нерелевантная норма в ответе** — см. живой прогон. Лечится расширением
     корпуса (перерасчёт — раздел IX ПП №354, отдельная задача KB) и выбором
     модели (CLASSIFY-003). До этого **вопросы о перерасчёте на демонстрации
     не показывать**. Отдельный дешёвый вариант, который стоит померить:
     требовать у подтверждённых фрагментов минимальный score retrieval.
  2. Семантический кэш ответов о правах.
  3. Живой прогон в MAX на VM — вместе с батч-деплоем основного разработчика.
