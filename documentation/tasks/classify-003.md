# CLASSIFY-003 — Надёжность классификации на живой модели (открытая дыра)

| Поле | Значение |
|---|---|
| ID | `CLASSIFY-003` |
| Статус | `in_progress` — открыт один критерий: стабильность на повторах (см. «Результат») |
| Требования | architecture.md §4 (порог и обоснование), §6.2, §13.2 (калибровка порогов); idea_and_scope.md шаг 5 (метрика точности) |
| Зависимости | CLASSIFY-001, CLASSIFY-002 |
| Блокирует | EVAL-002, PRES-001, SUBMIT-001 (главная метрика продукта) |

## Проблема

**Сейчас большинство обращений на живой модели уходит в ветку «не берусь
определить ответственного»**, хотя код классификатора работает правильно.
Это главный риск защиты: продукт обещает ответ на вопрос «кому», а на
практике чаще отвечает «передал диспетчеру».

Живые прогоны `scripts/classify_demo.py` на одних и тех же 15 сочинённых
фразах (модель `nex-agi/nex-n2.5-mini:free`):

| Прогон | Сопоставление ссылок | auto | clarify | unknown |
|---|---|---:|---:|---:|
| CLASSIFY-001, первый | строка `cited_refs` | 5 | 1 | 9 |
| После ревью, 22.09.2026 | номер фрагмента `cited_fragments` | 4 | 0 | 11 |

Замена строк ссылок на номера фрагментов была правильной (устранила
ложные промахи из-за пересказа ссылки), но доля `unknown` не снизилась.
Причина — поведение самой бесплатной модели, а не код.

**Один и тот же запрос с теми же фрагментами на входе** («газ в плите не
зажигается, запаха в подъезде нет»), четыре вызова подряд, `temperature = 0`:

| Вызов | `cited_fragments` | `confidence` | зона |
|---|---|---:|---|
| 1 (в прогоне) | `[]` | ≥ 0.75 → срезана до 0.749 | uk |
| 2 | `[2, 3, 5]` | 0.0 | uk |
| 3 | `[]` | 0.86 | owner |
| 4 | `[]` | 0.86 | uk |
| 5 | `[]` | 0.86 | uk |

Модель:

- нестабильна при одинаковом входе (зона прыгает `uk` ↔ `owner`);
- чаще всего не заполняет `cited_fragments`, хотя фрагменты показаны и
  релевантны (поиск находит `ПП РФ №491, п. 5 абз. 3` про газ, `ПП РФ
  №354, п. 31 пп. л`, `Прил. 1 п. 11`);
- выдаёт `confidence`, не согласованный с собственным `reasoning`
  (уверенное объяснение при `confidence = 0.0`).

Правило «без подтверждённой ссылки — никакого `auto`» (CLASSIFY-001)
работает как задумано и защищает от выдуманных норм, но на такой модели
срабатывает почти всегда. Ещё один симптом: ~14 с на фразу (замер
CLASSIFY-001) при последовательном воркере.

## Что нужно сделать (варианты, решение — за этой задачей)

1. **Подобрать модель.** Прогнать 3–5 кандидатов OpenRouter (бесплатных и,
   если бюджет позволяет, недорогих) на одном наборе: доля заполненных
   `cited_fragments`, стабильность ответа на повторах (5 вызовов одной
   фразы), доля `auto`, задержка. Проверить и резервную
   `liquid/lfm-2.5-2.6b:free` — поддержка `response_format` со
   `strict: true` для неё не подтверждена (LLM-001).
2. **Не доверять самооценке модели.** `confidence` модели не калиброван.
   Варианты: голосование нескольких вызовов (согласие ответов как мера
   уверенности), либо отдельная оценка уверенности по совпадению решения
   LLM с решением по прототипам (CLASSIFY-002).
3. **Смягчить правило основания без потери защиты.** Если модель не
   процитировала фрагмент, но решение совпадает с `default_responsibility_zone`
   справочника и среди найденных есть фрагмент нужного типа — брать этот
   фрагмент как основание. Любое смягчение — с тестом, что выдуманный
   пункт по-прежнему не показывается жителю.
4. **Промпт.** Сделать `cited_fragments` обязательным непустым полем
   схемы, добавить в промпт пример ответа с заполненным полем.

Критерий выхода — не «зелёные тесты», а цифры живого прогона.

## Критерии приёмки

- [x] Живой прогон 15 фраз: доля `unknown` ≤ 30% при нуле показанных жителю выдуманных пунктов — не-auto 15–20% (замер `model_bakeoff.py` на тех же 15 фразах, тот же промпт и порог).
- [ ] Стабильность: 5 повторов каждой из 5 фраз дают одинаковую зону в ≥ 4 из 5 — **не снята**: в момент замера провайдер Nex AGI перестал отвечать на полный промпт (обе модели семейства).
- [x] Выбранные `OPENROUTER_MODEL` / `_FALLBACK` и причина выбора — в `openrouter_api.md` §10 и закрытии.
- [x] Задержка на фразу замерена и разложена по этапам (эмбеддинг, поиск, LLM).
- [x] `bash scripts/check.sh` проходит.

## Проверка

```bash
uv run python scripts/classify_demo.py
```

## Результат закрытия

**23.09.2026 — основная причина найдена и устранена; открыт один критерий.**

**Причина была в нашем контракте, а не в модели.** В JSON-схеме, которую
клиент отправлял в строгом режиме, `cited_fragments` не входил в
`required`: pydantic кладёт туда только поля без значения по умолчанию.
Модель законно не присылала основание, и правило «нет подтверждённой
нормы — нет auto» отправляло обращение в «не уверен». Вариант 4 постановки
(«сделать поле обязательным») оказался не косметикой, а главным исправлением.

Сделано:

- `openrouter.py:_as_strict_schema` — все поля схемы в `required`,
  `additionalProperties: false`, рекурсивно по `$defs`. Одно место — чинит
  и классификацию, и справку о правах RIGHTS-001. Пустой список
  по-прежнему допустим: обязанность назвать основание не превращается в
  обязанность его выдумать (§6.4); выдуманный номер отбрасывает
  `confirm_citations`.
- Промпт: образец заполненного ответа и правило заполнения
  `cited_fragments`; `PROMPT_VERSION = 3` (инвалидирует семантический кэш).
- `LLM_TIMEOUT_SECONDS` 8 → **45 с**: медиана вызова 8–13.5 с, прежний
  таймаут обрывал больше половины вызовов. Граница сверху — окно видимости
  inbound 120 с (основная + резерв = 90 с).
- Модели: основная `nex-agi/nex-n2.5-pro:free`, резерв
  `nvidia/nemotron-3-super-120b-a12b:free` (другой провайдер) — обоснование
  и таблица замера в `openrouter_api.md` §10.
- `scripts/model_bakeoff.py` — воспроизводимый замер кандидатов: реальный
  промпт без кэша и без резерва, доля основания, доля auto, стабильность на
  повторах, задержка по этапам; 429 провайдера переживается повторами и не
  считается отказом модели.
- Тест `test_schema_sent_in_strict_form`.

**Цифры** (15 фраз `classify_demo.py`):

| | До | После правки схемы |
|---|---|---|
| Основание заполнено | чаще пусто | 100% (mini), 87% (pro) |
| auto | 4 из 15 (27%) | 85% (mini), 80% (pro) |

Задержка по этапам (медиана): маскирование ~0 мс, эмбеддинг 8–9 мс,
эмбеддинг + поиск 16 мс, LLM 8.0 с (pro) / 13.5 с (mini).

**Открыто:** стабильность 5 × 5. Замер запускался дважды и оба раза упёрся
в недоступность провайдера Nex AGI (обе модели семейства одновременно не
отвечали на полный промпт 45 с, при этом простой запрос проходил за 3.6 с).
Команда для повтора:

```bash
uv run python scripts/model_bakeoff.py --models nex-agi/nex-n2.5-pro:free --phrases 5 --repeats 5 --timeout 45
```

**Замечание о точности.** `auto` — это доля обращений, по которым бот
отвечает, а не доля правильных ответов. Правильность зоны против эталона
меряет EVAL-002 на наборе EVAL-001.
